# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'French Verbs Quiz' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for French Verbs Quiz
	pod 'Firebase/Core'
    	pod 'Google-Mobile-Ads-SDK'
	pod 'Charts'
  target 'French Verbs QuizTests' do
    inherit! :search_paths
    pod 'Firebase'
    # Pods for testing
  end

  target 'French Verbs QuizUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
